# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env

# LanceDB local store
data/lancedb/

# Downloaded HF datasets (large binaries, re-downloadable)
data/tuvanphapluat/

# Bản tải về từ nút download của scripts/tuvanphapluat_viewer.py — trùng từng ký tự với
# eval/tuvanphapluat_selected.jsonl (chỉ khác kiểu xuống dòng), nên chỉ bản kia được version
data/evaluate/

# Cache sitemap vbpl.vn — không phải bản chụp nguồn, tải lại được, ~15-20 MB
data/raw/vbpl/.sitemap_cache/

# Bản thô 3 tab + log mẻ cào. Chỉ `corpus/` được version: diff của nó cho thấy nguồn đổi gì
# hoặc parser đổi gì. Bản thô ~2,7 MB/lượt, không ai đọc diff, và dựng lại được từ
# research/crawl_list.txt — file đó mới là nguồn tái tạo, phải luôn phủ đủ corpus/.
data/raw/vbpl/raw/
data/raw/vbpl/crawl-report.json

# Bản canonical tải về trước khi ghi đè Storage — ảnh chụp của môi trường, không phải nguồn
data/backup/

# Cấu hình Claude Code riêng từng máy (chứa đường dẫn tuyệt đối của máy đó)
.claude/settings.local.json

# Phiên brainstorm (mockup HTML + state của server xem thử), không phải sản phẩm
.superpowers/

# Next.js
web/node_modules/
web/.next/
web/out/
web/.env.local

# OS
.DS_Store
Thumbs.db

# Ghi chu nghien cuu ca nhan (chua cong bo)
docs/SCHEMA_KG.md

# Hợp đồng thật (GraphCompliance POC) + nhãn vàng bóc từ chúng — không commit
docs/compliance/
eval/compliance/*.jsonl
eval/compliance/*.md
eval/compliance/*.cache.json
# ... trừ bộ synthetic sinh từ luật công khai, đã người duyệt (T30)
!eval/compliance/synthetic_pilot.jsonl
!eval/compliance/synthetic_pilot.kq.jsonl
!eval/compliance/synthetic_pilot.md
!eval/compliance/t29_lat.jsonl
!eval/compliance/synthetic_mo_rong.jsonl
!eval/compliance/synthetic_mo_rong.kq.jsonl
eval/compliance/synthetic_pilot.html
eval/compliance/synthetic_mo_rong.html
# Bản xuất từ trang duyệt HTML — quyết định đã gộp vào synthetic_mo_rong.jsonl (trường duyet)
data/synthetic_mo_rong.ketqua.json
